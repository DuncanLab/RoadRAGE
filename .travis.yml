language: objective-c
osx_image: xcode10.1
rvm:
- 2.2         #see before_install for getting RVM stable.

env:
- TRAVIS_TAG="Deploy-2018.3.5f1" PROJECT_NAME="RoadRage" UNITY_PROJECT_PATH="." DEPLOY_UNITYPACKAGE="path set in deploy script"

cache:
  directories:
    - $UNITY_DOWNLOAD_CACHE

    
before_install:
- chmod a+x ./BuildScripts/*.sh

install:
- cat /etc/hosts
- ./BuildScripts/install-unity.sh

script:
- ./BuildScripts/build.sh
# - ./BuildScripts/run-tests.sh

# after_success:
# - ./BuildScripts/export-unity-package.sh

# before_deploy:
# - DEPLOY_UNITYPACKAGE="$(ls -a release/*.zip | head -n 1)"

# deploy:
#   provider: releases
#   api_key: $GITHUB_TOKEN
#   file: $DEPLOY_UNITYPACKAGE
#   skip_cleanup: true
#   overwrite: true
#   on:
#     tags: true 
